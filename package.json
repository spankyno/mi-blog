{
  "name": "mi-blog-temp",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && rm -f dist/server/wrangler.json"
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.10",
    "astro": "^6.1.8"
  },
  "overrides": {
    "vite": "^7"
  },
  "devDependencies": {
    "wrangler": "^4.84.0"
  }
}
